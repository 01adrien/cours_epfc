

- MULTICAST: (FF00::/8  == 224.0.0.0/4)
  - Well-know adresses => concernent des groupes de machines (tous les routeurs (FF02::2), tous les nodes (FF02::1))
  - Solicited-node     => transforme unicast en multicast.." (utilise par NDP)   

- ANYCAST:
  - le plus proche routeur


LINK LOCAL:

64 premiers bits

adresse link local generee par le PC au moment ou il entre sur le reseau

ndp == arp
